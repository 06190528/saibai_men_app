// import 'package:flutter_riverpod/flutter_riverpod.dart';

// class UserDataObserver extends ProviderObserver {
//   @override
//   void didUpdateProvider(ProviderBase provider, Object? newValue) {
//     super.didUpdateProvider(provider, newValue);
//     if (provider == userDataProvider) {
//       print('UserData has been updated');
//       // ここでsaveUserDataFromLocalToProviderを呼び出す
//       // (注意: この部分では `ref` にアクセスできないため、別の方法を検討する必要がある)
//     }
//   }
// }
